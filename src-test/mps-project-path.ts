/**
 * Path relative to this repo's root that points to an MPS project containing
 * (at least) the Json and JsonSchema languages, and models written in those languages.
 */
export const mpsProjectPath = "tmp/mpsProject"

